import React from 'react';
import './App.css';

const Header = () => {
  return (
    <header class="header">
      <h1 classe="header-title">Monitoria React</h1>
      <nav>
        <ul>
          <li class="header-link">Contato</li>
        </ul>
      </nav>
    </header>
  );
};

export default Header;
